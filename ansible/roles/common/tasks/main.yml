---
- name: Installer les paquets communs
  apt:
    name:
      - curl
      - wget
      - git
      - unzip
      - python3
      - python3-pip
    state: present
    update_cache: yes

- name: Installer le module kubernetes pour Python
  pip:
    name: kubernetes
    state: present
